import react from 'reat';
import PropTypes from 'prop-types';

import {} from './styles';

function Card () {
    return (
        <></>
    );
}

Card.propTypes = {
};

Card.defaultProps = {
};

export default Card;